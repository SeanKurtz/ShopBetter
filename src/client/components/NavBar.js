import React from 'react';


const NavBar = () => (
  <nav className="navbar">
    <p className="text--large text--light">Shop Administrator</p>
    <button>Log Out</button>
  </nav>
);

export default NavBar;
